name: Manual Workflow

on:
  push:
    branches:
      main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "👋 This workflow was triggered manually!"
